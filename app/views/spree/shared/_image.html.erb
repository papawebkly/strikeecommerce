<% size ||= :mini %>
<% itemprop ||= nil %>

<% if image && image.attachment? %>
  <% if itemprop %>
    <%= image_tag image.attachment(size), itemprop: itemprop, class: 'img-fluid', style: 'width:100%' %>
  <% else %>
    <%= image_tag image.attachment(size), class: 'img-fluid', style: 'width:100%' %>
  <% end %>
<% else %>
  <span class="image-placeholder <%= size %>"></span>
<% end %>
