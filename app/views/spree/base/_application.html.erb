 <%= render partial: 'spree/shared/mini_cart' %>
 <%= render partial: 'spree/shared/main_menu' %>

<%= render partial: 'spree/shared/header' %>
<div class="main-container">
    <div id="wrapper" class="b-wrapper" data-hook>

        <%#= taxon_breadcrumbs(@taxon) %>

        <%#= render partial: 'spree/shared/sidebar' if content_for? :sidebar %>

        <div id="content" class="columns <%= !content_for?(:sidebar) ? "sixteen" : "twelve omega" %>" data-hook>
            <%#= flash_messages %>
            <%= yield %>
        </div>

        <%= yield :templates %>

    </div>
    <%= render partial: 'spree/shared/footer' %>

</div>